#!/usr/bin/env bash
# script to enable connection on port 80
#echo "include /etc/nginx/sites-enabled/*;" | sudo tee /etc/nginx/nginx.conf;
sudo sed -i '/http {/a include \/etc\/nginx\/sites-enabled\/*;';
sudo ufw allow 80;
sudo ufw allow "Nginx HTTP";
sudo service nginx restart;
