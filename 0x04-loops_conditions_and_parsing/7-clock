#!/usr/bin/env bash
# This script count time in hour from 0 to 24
hour="0"
min="1"
while [ "$hour" -le 11 ]
do
	echo Hour: $hour
	while [ "$min" -lt 60 ]
	do
		echo $min
		min=$((min + 1))
	done
	min=1
	hour=$((hour + 1))
done
echo Hour: $hour
